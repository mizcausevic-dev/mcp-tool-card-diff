{
  "tool_card_version": "0.1",
  "tool": {
    "server_id": "research-mcp",
    "name": "delete-document",
    "version": "2.0.0",
    "mcp_server_uri": "https://example.com/mcp/research",
    "description": "Permanently remove an indexed document and its embeddings."
  },
  "schema": {
    "input_schema_inline": {
      "type": "object",
      "properties": { "doc_id": { "type": "string" }, "force": { "type": "boolean" } },
      "required": ["doc_id", "force"]
    }
  },
  "safety": {
    "side_effect_class": "destructive",
    "external_systems": ["s3.amazonaws.com"],
    "reversible": false,
    "rate_limited": false,
    "pii_exposure": "high",
    "secrets_exposure": "reads_secret_material",
    "human_approval_required": false,
    "refusal_modes": []
  },
  "tested_with": [
    { "provider": "openai", "model": "gpt-4o-mini", "passed": false }
  ],
  "audit": { "log_locations": [], "retention_days": 90 }
}
